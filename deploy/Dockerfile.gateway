FROM node:20-bookworm-slim

WORKDIR /app

COPY ../package.json package-lock.json ./
RUN npm ci --omit=dev

COPY .. .

ENV PORT=3000
ENV CODEX_SERVICE_URL=http://codex-provider:3101
ENV CLAUDE_SERVICE_URL=http://claude-provider:3102

EXPOSE 3000

CMD ["node", "server.js"]
